# Pinned for reproducible deploys. Unpinned ranges mean a breaking upstream
# release can kill a Render build with no change on our side. These exact
# versions are verified to import main.py and boot uvicorn cleanly.
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
python-dotenv==1.2.3
supabase==2.31.0
PyPDF2==3.0.1
pillow==12.3.0
openai==3.7.0
pytest==9.1.1
